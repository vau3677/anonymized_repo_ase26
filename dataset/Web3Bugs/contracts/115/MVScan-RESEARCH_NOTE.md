Research note for auditors:

- `supervaults` is a repo that we evaluated. We did not evaluate `core`.
- This should help clear up any confusion regarding `/115` belonging to both the `excluded` and `included` repository folders.